#include <stdio.h>
#include <math.h>

/*
Funzione che copia tutti gli elementi dell'array sorgente nell'array destinazione spostandoli di una posizione a sinistra. L'elemento più a sinistra verrà spostato a destra. Ad esempio, dall'array:

src = { 1, 2, 3, 4, 5 }

vogliamo ottenere l'array:

dst = { 2, 3, 4, 5, 1 }
*/
void leftShift(int src[], int dst[], int n) {
  // TODO
}

/*
Come la funzione precedente ma gli elementi vengono spostati nell'array di partenza e non in un nuovo array.
*/
void leftShiftInPlace(int src[], int n) {
  // TODO
}

/*
Funzione che copia tutti gli elementi dell'array sorgente nell'array destinazione spostandoli di una posizione a destra. L'elemento più a destra verrà spostato a sinistra. Ad esempio, dall'array:

src = { 1, 2, 3, 4, 5 }

vogliamo ottenere l'array:

dst = { 5, 1, 2, 3, 4 }
*/
void rightShift(int src[], int dst[], int n) {
  // TODO
}

/*
Come la funzione precedente ma gli elementi vengono spostati nell'array di partenza e non in un nuovo array.
*/
void rightShiftInPlace(int src[], int n) {
  // TODO
}
